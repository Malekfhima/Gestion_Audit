// ==============================================
// MODULE: PROCESSES - CONTROLLER LAYER
// ==============================================
// Endpoints: POST /, GET /, GET /:id, PUT /:id, DELETE /:id, GET /site/:siteId, POST /:id/activate, POST /:id/deactivate
// Auth: All require authentication, write operations require ADMIN or MANAGER
