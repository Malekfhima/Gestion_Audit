// ==============================================
// MODULE: SITES - CONTROLLER LAYER
// ==============================================
// Endpoints: POST /, GET /, GET /:id, PUT /:id, DELETE /:id, POST /:id/activate, POST /:id/deactivate
// Auth: All require authentication, write operations require ADMIN or MANAGER
